import "./styles.css";
import GameComponent from "./GameComponent";

export default function App() {
  return (
    <div className="App">
      <GameComponent />
    </div>
  );
}
// 1. how to add a canvas to react
// 2. draw a grid to the canvas (not infinite grid)
// 3. update the logic of the pixel
